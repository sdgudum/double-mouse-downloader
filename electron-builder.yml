appId: 'moe.moyu.double-mouse-downloader'
productName: 鼠鼠下载器

win:
  target:
    - target: 7z
      arch:
        - x64

directories:
  buildResources: build-resources

files:
  - ./build/**/*

extraResources:
  - ./bin/${platform}/${arch}/*
